import { LazyImgeDirective } from './lazy-imge.directive';

describe('LazyImgeDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyImgeDirective();
    expect(directive).toBeTruthy();
  });
});
